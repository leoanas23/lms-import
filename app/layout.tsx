import './globals.css';
export const metadata = { title: 'GO Import — LMS to GrowthWheel Online' };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="en"><body>{children}</body></html>;
}
